#include "Layer.h"
#include "Activation.h"

Activation::Activation(int in_features) {

}
void Activation::forward(float* input, float* output) {
    
}
void Activation::backward(float* input, float* output) {
    
}
void Activation::update(float* input, float* output) {
    
}
