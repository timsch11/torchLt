#include "Layer.h"

class Model {
    private:
        Layer layers;
        int layers_count;

    public:
        Model(Layer* layers, int count) : layers(layers), layers_count(count) {

            this->layers_count = 0;
        }
        void predict(float* input) {
            for 
        }
};

